/**
 * Adapter loader — import this module once at server startup to register
 * all niche adapters before any /api/sync calls are made.
 *
 * Add a new niche package import here whenever a new niche pack is created.
 */
import "@niche-factory/niches";
