// Pinterest Poster niche — no external data sources needed.
// Credentials (PINTEREST_ACCESS_TOKEN, PINTEREST_BOARD_ID) are stored
// as environment variables and used server-side in the API route.
export {};
