---
name: send_email
description: Send a plain-text email via the Resend API. Use for notifications, summaries, or reports generated by the agent.
inputs:
  to:
    type: string
    description: Recipient email address
    required: true
  subject:
    type: string
    description: Email subject line
    required: true
  body:
    type: string
    description: Plain text email body
    required: true
  from:
    type: string
    description: Sender email address (default depends on Resend domain config)
    required: false
when_to_use: When you need to send an email report, alert, or summary to a user. Requires RESEND_API_KEY in settings and a verified sending domain in Resend.
---

# send_email

Sends a transactional email via Resend. The from address must be from a domain verified in your Resend account.

## Required API Key

Set `RESEND_API_KEY` in the admin Settings page. Get a free key at https://resend.com (100 emails/day free tier).
